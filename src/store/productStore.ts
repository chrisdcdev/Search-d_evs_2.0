/* import { create } from "zustand"

interface StoreState {
  listFilterItems: Product[];
  setFilter: (listFilter: Product[]) => void;
}

const useStore = create<StoreState>((set) => ({
  listFilterItems: [],
  setFilter: (listFilter) => set({ listFilterItems: listFilter }),
}));

export default useStore; */